import type { SymptomAnalysisOutput } from '@/ai/flows/personalized-symptom-analysis';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Lightbulb, Info } from 'lucide-react';

type AnalysisResultsProps = {
  results: SymptomAnalysisOutput;
};

export function AnalysisResults({ results }: AnalysisResultsProps) {
  return (
    <div className="space-y-8">
       <h2 className="text-3xl font-bold text-center font-headline">Your Personalized Analysis</h2>
      <Card className="bg-background/70 border-primary border-2 shadow-xl">
        <CardHeader className="flex flex-row items-center gap-4">
          <Info className="h-8 w-8 text-primary" />
          <CardTitle className="font-headline text-2xl">Potential Causes</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="text-lg">{results.potentialCauses}</p>
        </CardContent>
      </Card>

      <Card className="bg-background/70 border-accent border-2 shadow-xl">
        <CardHeader className="flex flex-row items-center gap-4">
          <Lightbulb className="h-8 w-8 text-accent" />
          <CardTitle className="font-headline text-2xl">Personalized Advice</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="text-lg whitespace-pre-line">{results.personalizedAdvice}</p>
        </CardContent>
      </Card>

      <Card className="bg-destructive/10 border-destructive">
          <CardHeader>
            <CardTitle className="text-destructive font-headline">Disclaimer</CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-destructive/90">
              This analysis is generated by an AI and is for informational purposes only. It is not a substitute for professional medical diagnosis or treatment. Please consult a qualified healthcare provider for any health concerns.
            </p>
          </CardContent>
      </Card>
    </div>
  );
}
